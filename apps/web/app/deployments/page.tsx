import { Shell } from '@/components/shell';
export default function DeploymentsPage() { return <Shell><h1 className="text-3xl font-bold">Deployments</h1><p className="mt-2 text-slate-600">Monitor workflow status, task progress, outputs, and orchestrator execution details.</p></Shell>; }
